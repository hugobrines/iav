# Requirements for the TCN anomaly detection project
# CPU/GPU note: Install `tensorflow` (will pick CPU or GPU build depending on platform).
# For GPU, ensure compatible CUDA/cuDNN are installed for the chosen `tensorflow` version.

numpy>=1.24
pandas>=1.5
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.2
tensorflow>=2.12,<2.15
h5py>=3.8
joblib>=1.2
statsmodels>=0.14
tqdm>=4.64

# Optional / dev
ipython
pytest
